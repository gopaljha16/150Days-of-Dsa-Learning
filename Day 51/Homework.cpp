#include <iostream>
#include <fstream>
#include <algorithm>
#include <vector>
using namespace std;

int main(){
    ifstream fin;
    fin.open("test.txt");
    
     vector<int>ans(5);

     ofstream fout;
     fout.open("")
     while(!fin.eof()){
        
     }
}